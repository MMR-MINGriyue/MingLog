{
  "name": "@minglog/search",
  "version": "0.1.0",
  "description": "Search engine for MingLog",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "echo 'Search package build skipped for now'",
    "dev": "tsup --watch",
    "test": "vitest",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "fuse.js": "^7.0.0",
    "@minglog/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.1",
    "vitest": "^1.0.4",
    "typescript": "^5.3.0"
  }
}
