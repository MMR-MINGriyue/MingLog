{
  "name": "@minglog/core",
  "version": "0.1.0",
  "description": "Core business logic and data structures for MingLog",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "echo 'Core package build skipped for now'",
    "dev": "tsup --watch",
    "test": "vitest",
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "nanoid": "^5.0.4",
    "date-fns": "^2.30.0",
    "zod": "^3.22.4",
    "immer": "^10.0.3",
    "@minglog/database": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.1",
    "vitest": "^1.0.4",
    "typescript": "^5.3.0"
  }
}
